package {{ .Package }}

import (
    "net/http"
)

type {{ .Name }} struct{}

func (h *{{ .Name }}) Handle(w http.ResponseWriter, r *http.Request) {
    w.WriteHeader(http.StatusOK)
    w.Write([]byte("ok"))
}
