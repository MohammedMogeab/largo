# {{ .AppName }}

Generated by LarGo (`largo new {{ .AppName }}`).

## Run

1) Copy `.env.example` to `.env` and adjust values as needed.

2) Start the server:

    go run ./cmd/server

3) Try routes:

- 200 OK

    curl -s http://localhost:8080/ | jq .

- 200 OK with param

    curl -s http://localhost:8080/hello/alice | jq .

- 422 validation error

    curl -s -X POST http://localhost:8080/signup -H 'Content-Type: application/json' -d '{"email":"bad","name":"a"}' | jq .

- 201 created

    curl -s -X POST http://localhost:8080/signup -H 'Content-Type: application/json' -d '{"email":"a@b.com","name":"Alice"}' | jq .
